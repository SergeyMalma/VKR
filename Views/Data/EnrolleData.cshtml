

<div id="t2"></div>
<div id="t3"></div>

<span>Выберите интерисующее направление</span>

<select name="s1">
    <option value="t1">09.03.02</option>
    <option value="t2">10.03.01</option>
    <option value="t3">09.03.03</option>
    <option value="t4">09.03.04</option>
    <option value="t5">10.05.01</option>
    <option value="t6">38.03.01</option>
    <option value="t7">38.05.01</option>
    <option value="t8">01.03.05</option>
    <option value="t9">03.03.01</option>
</select>
<br>
<br>
<span>Список абитуриентов, подавших заявления</span>
<br>
<br>
<body>
    <div id="table_div1"></div>
</body>
@section Scripts
{
    <script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>
    <script type="text/javascript">
        google.charts.load('current', { 'packages': ['table'] });
        google.charts.setOnLoadCallback(drawTable);

        function drawTable() {
            var jsonData = $.ajax({
                url: "/Data/OnGetChartDataEnrolleeData1",
                dataType: "json",
                async: false
            }).responseText;
            var data = new google.visualization.DataTable(jsonData);


            var table = new google.visualization.Table(document.getElementById('table_div1'));

            table.draw(data, { showRowNumber: true, width: '100%', height: '100%', pageSize: '10' });
        }
    </script>
    <script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>

    <script type="text/javascript">

        // Load the Visualization API and the corechart package.
        google.charts.load('current', { 'packages': ['corechart'] });

        // Set a callback to run when the Google Visualization API is loaded.
        google.charts.setOnLoadCallback(drawChart);

        // Callback that creates and populates a data table,
        // instantiates the pie chart, passes in the data and
        // draws it.
        function drawChart() {
            var jsonData2 = $.ajax({
                url: "/Data/OnGetChartDataEnrollee3",
                dataType: "json",
                async: false
            }).responseText;
            // Create the data table.
            var data = new google.visualization.DataTable(jsonData2);

            // Set chart options
            var options = {
                'title': 'Текущие проходные баллы',
                'width': 1300,
                'height': 500,

            };

            // Instantiate and draw our chart, passing in some options.
            var chart = new google.visualization.BarChart(document.getElementById('t3'));
            chart.draw(data, options);
        }
    </script>
    <script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>

    <script type="text/javascript">

        // Load the Visualization API and the corechart package.
        google.charts.load('current', { 'packages': ['corechart'] });

        // Set a callback to run when the Google Visualization API is loaded.
        google.charts.setOnLoadCallback(drawChart);

        // Callback that creates and populates a data table,
        // instantiates the pie chart, passes in the data and
        // draws it.
        function drawChart() {
            var jsonData2 = $.ajax({
                url: "/Data/OnGetChartDataEnrollee2",
                dataType: "json",
                async: false
            }).responseText;
            // Create the data table.
            var data = new google.visualization.DataTable(jsonData2);

            // Set chart options
            var options = {
                'title': 'Количество заявлений на текущий момент',
                'width': 1300,
                'height': 500,

            };

            // Instantiate and draw our chart, passing in some options.
            var chart = new google.visualization.ColumnChart(document.getElementById('t2'));
            chart.draw(data, options);
        }
    </script>
}